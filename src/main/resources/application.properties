spring.application.name=library

# Database Connection Settings
# spring.datasource.url=jdbc:postgresql://host.docker.internal:5432/postgres
spring.datasource.url=jdbc:postgresql://192.168.1.13:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
# spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Optional: Connection Pool Settings (HikariCP)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10

# Nonaktifkan CSRF untuk mempermudah pengujian API
spring.security.filter.order=10

# Swagger Configuration
# springdoc.api-docs.path=/v3/api-docs
# springdoc.swagger-ui.path=/swagger-ui.html

# Spring Security Default User (for testing purposes)
# spring.security.user.name=admin
# spring.security.user.password=admin123
# spring.security.user.roles=USER

# JWT
jwt.secret=8lH!2eN9Vf4pZs0qYx3Rr7Uw@jKc5bLm
jwt.expiration-ms=3600000


# Spring security: allow actuator/swagger while developing
springdoc.api-docs.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
server.port=8081
